services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: code-sherpa-cloudflared
    restart: unless-stopped
    network_mode: host
    depends_on:
      mcp:
        condition: service_healthy
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
